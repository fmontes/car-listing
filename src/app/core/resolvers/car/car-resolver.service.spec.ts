import { TestBed } from '@angular/core/testing';
import { Car } from '@models/api.model';
import { CarResolverService } from '@core/resolvers/car/car-resolver.service';

xdescribe('Service: CarResolver', () => {
    let service: CarResolverService;

    // beforeEach(() => {
    //     TestBed.configureTestingModule({
    //         providers: [CarResolverService]
    //     });
    //     service = new CarResolverService();
    // });

    // it('should get a car', (done) => {
    //     service.resolve().subscribe((car: Car) => {
    //         expect(cars.map((car) => car.brand)).toEqual();
    //         done();
    //     });
    // });
});
