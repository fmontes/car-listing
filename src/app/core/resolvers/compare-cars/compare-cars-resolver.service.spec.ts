import { TestBed, async, inject } from '@angular/core/testing';
import { CompareCarsResolverService } from './compare-cars-resolver.service';

describe('Service: CompareCarsResolver', () => {
    // let service: CompareCarsResolverService;
    // beforeEach(() => {
    //     TestBed.configureTestingModule({
    //         providers: [CompareCarsResolverService]
    //     });
    //     service = new CompareCarsResolverService();
    // });
    // it('should get a car', (done) => {
    //     service.resolve().subscribe((car: Car) => {
    //         expect(cars.map((car) => car.brand)).toEqual();
    //         done();
    //     });
    // });
});
